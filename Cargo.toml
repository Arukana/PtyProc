[package]
name = "example"
version = "0.1.0"
build = "build.rs"

[features]
lints = ["nightly"]
nightly = []
unstable = []

[build-dependencies]
lalrpop = "0.12.0"

[dependencies]
lalrpop-util = "0.12.0"

[dependencies.libc]
version = "*"
